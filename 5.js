let str1 = "Yasir";
let str2 = " Ammar";
let str3 = str1 + str2;
console.log(str3);

console.log("1"+2);
// when 1st str and 2nd number added together then the result is string because when a string is added to a number, the number is converted to a string and concatenated with the original string. So in this case, "1" + 2 results in "12".

console.log(1+"2");
// when 1st number and 2nd str added together then the result is string because when a number is added to a string, the number is converted to a string and concatenated with the original string. So in this case, 1 + "2" results in "12".

console.log(1+2+"3");
// when 1st number and 2nd number added together then the result is number because when two numbers are added together, the result is a number. So in this case, 1 + 2 results in 3. Then when the result is added to the string "3", the number is converted to a string and concatenated with the original string. So in this case, 3 + "3" results in "33".

console.log('1'+2+3);
// when 1st str and 2nd number and 3rd number added together then the result is string because when a string is added to a number, the number is converted to a string and concatenated with the original string. So in this case, "1" + 2 + 3 results in "123".




